{
    "extends": "../tsconfig.base",
    "compilerOptions": {
        "removeComments": true
    },
    "include": [
        "./**/*" // . - Look in current directory ** - Look recursively in all subdirectories *.ts look for all files with .ts extension
    ]
}